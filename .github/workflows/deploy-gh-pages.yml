name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # or your default branch

permissions:
  id-token: write
  contents: read
  pages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build App
        run: yarn build

      - name: Build Storybook
        run: yarn build-storybook -o dist/storybook

      - name: Setup MSW Service Worker
        run: |
          # Initialize MSW in the root directory
          npx msw init dist/ --save
          
          # Copy the service worker to the storybook directory
          cp dist/mockServiceWorker.js dist/storybook/
          
          # Initialize MSW in the storybook directory as well
          npx msw init dist/storybook/ --save
          
          # Verify both files exist
          ls -la dist/mockServiceWorker.js
          ls -la dist/storybook/mockServiceWorker.js

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          artifact_name: github-pages
          token: ${{ secrets.GITHUB_TOKEN }}
